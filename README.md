# ibda-week4

1+1

# git add .
# git commit -m "dummy commit"
# git push 

# git branch feature -> yeni branch'i feature diye adlandırdık.
# git checkout feature -> branch'i değiştirdik. 

# git push origin feature -> feature branc'teki değişiklikleri push'lar
# önce main branch'e gel sonra git merge feature de feature'daki değişiklikleri main branch'e 
#aktarmış olduk. 